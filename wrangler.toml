name = "raksha-bandhan-astro"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = "dist"

# Secret variables like GEMINI_API_KEY should NOT be committed in plain text.
# For local dev: Wrangler reads from .dev.vars (which is in .gitignore).
# For Cloudflare Pages deployment: Run `npx wrangler pages secret put GEMINI_API_KEY`
